
#!/bin/bash

RET=$(echo -e "shutdown\nreboot\nlogout\ncancel" | dmenu -l 5 -p "Select Action:")

case $RET in
    shutdown) systemctl poweroff ;;
    reboot) systemctl reboot ;;
    logout) xdotool key "super+shift+q" ;;
    *) ;;
esac
